{
  "name": "disable-admin-ad",
  "version": "1.0.0",
  "description": "Hide advertising blocks in WordPress admin via XPath rules. Selection overlay, Vite build, SCSS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint:js": "eslint \"assets/js/**/*.js\"",
    "lint:scss": "stylelint \"assets/scss/**/*.scss\"",
    "lint": "npm run lint:js && npm run lint:scss"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "sass": "^1.75.0",
    "eslint": "^9.0.0",
    "@eslint/js": "^9.0.0",
    "eslint-plugin-es-x": "^7.6.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard-scss": "^13.0.0",
    "stylelint-scss": "^6.0.0"
  }
}

